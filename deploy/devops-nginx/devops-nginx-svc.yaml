kind: Service
apiVersion: v1
metadata:
  name: devops-nginx-svc02
  namespace: ks-devops-test
  labels:
    app: devopstest
    container: devops-nginx-02
    group: devopstest
spec:
  ports:
    - name: devops-nginx
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: devopstest
    container: devops-nginx-02
    group: devopstest
  sessionAffinity: None
